import { FC } from "react";
import { Box, VStack } from "@chakra-ui/react";

const SideBar: FC = () => {
  return (
    <VStack>
      <Box>Nav Menu</Box>

      <Box>Actions</Box>
    </VStack>
  );
};

export default SideBar;
