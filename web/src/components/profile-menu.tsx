import React from "react";

import { Box, Button } from "@chakra-ui/react";

const ProfileMenu = () => {
  return (
    <Box>
      <Button>login</Button>
    </Box>
  );
};

export default ProfileMenu;
