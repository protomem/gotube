import Settings from "@/feature/settings";
import RouterProvider from "@/feature/router/router-provider";

export default function App() {
  return (
    <Settings>
      <RouterProvider />
    </Settings>
  );
}
